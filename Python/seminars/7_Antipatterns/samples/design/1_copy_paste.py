# Обработка данных в модуле A
def process_data_a(data):
    result = []
    for item in data:
        if item > 0:
            result.append(item * 2)
    return result


# Обработка данных в модуле B
def process_data_b(data):
    result = []
    for item in data:
        if item > 0:
            result.append(item * 2)
    return result